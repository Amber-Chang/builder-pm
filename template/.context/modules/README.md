<!-- [AI-ASSISTED] 模板 by builder-pm -->
<!-- 我會這樣長:每做一個主要模組 → 在此放一支 <module-name>.md,記那個模組的知識。
     用到才寫(JIT),不是 day-0 一次列完。
     分工:
       - modules/<name>.md = 這個模組「怎麼運作」的知識
       - docs 的 SPEC-NNN = 這個功能的「契約 / 介面」定義
     context-growth 偵測器:會用這裡的 module 名,檢查有沒有對應 SPEC、有沒有寫進 SYSTEM。
     提醒:module 名取「夠獨特、≥ 6 字元」的名字(如 segmentation、journey-engine),
           太短太泛(如 id / type / rule)會讓偵測器的子字串比對誤判。 -->

# 模組知識（modules/）

day-0 這裡是空的。每做一個主要模組,放一支 `<module-name>.md`。
